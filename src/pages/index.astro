---
// Component Imports
import PreactCounter from '../components/preact/PreactCounter';
import SvelteCounter from '../components/svelte/SvelteCounter.svelte';
import BasicLayout from '../layouts/BasicLayout.astro';

// Full Astro Component Syntax:
// https://docs.astro.build/basics/astro-components/

const count = 0;
---

<BasicLayout>
			<PreactCounter count={count} client:only="preact">
				<h1>Hello from Preact!</h1>
				<h2>🦄</h2>
			</PreactCounter>
			<SvelteCounter count={count} client:only="svelte">
				<Fragment slot="slot1">
					<h1>Hello from Svelte!</h1>
					<h2>☄️</h2>
				</Fragment>
			</SvelteCounter>
</BasicLayout>
